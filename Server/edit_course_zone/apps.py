from django.apps import AppConfig


class CoursesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "edit_course_zone"
    verbose_name = "Курс"
    verbose_name_plural = "Курсы"
